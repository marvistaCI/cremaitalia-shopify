# Shopify CLI artifacts
.shopify/
*.shopifylocal
shopify.theme.toml

# Node / npm
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
package-lock.json.bak

# Build outputs
dist/
build/
.cache/

# Environment & secrets — NEVER commit these
.env
.env.local
.env.*.local
*.pem
*.key
*.p12

# Local dev-server launch script — carries the storefront password, never commit
dev.cmd

# IDE / editor noise
.vscode/
.idea/
.vs/
*.suo
*.user
*.sublime-project
*.sublime-workspace
*.code-workspace

# OS noise
.DS_Store
Thumbs.db
desktop.ini
$RECYCLE.BIN/
*.lnk

# Logs
*.log
logs/

# Temp & backup files
*.tmp
*.bak
*.swp
*~
.#*

# Local-only docs / notes (uncomment if you want to keep personal notes out of git)
# NOTES.md
# scratch/

# Generated Standard PDFs — renders of the Markdown sources (regenerate with
# docs/standards/render.py). Kept out of git to avoid binary churn on every edit;
# the .md is the source of truth. (Brand Standards' own PDF under brand-standards/
# is committed per the pdf-builder "save both" convention.)
/docs/standards/*.pdf
